# Copy to `.env` and fill in. Never commit `.env`.

# --- Future FVE / inverter API (placeholders) ---
# SOLAX_API_TOKEN=
# SOLAX_SERIAL_NUMBER=

# Optional: log level for stderr (e.g. debug | info | warn | error)
# LOG_LEVEL=info
